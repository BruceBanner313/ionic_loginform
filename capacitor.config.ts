import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'freshdirect.app',
  appName: 'Freshdirect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
